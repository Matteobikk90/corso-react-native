import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  infoCardContainer: {
    backgroundColor: "#f0f0f0",
    padding: 20,
    borderRadius: 10,
    marginBottom: 20,
    width: "90%",
  },
  infoCardTitle: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 10,
  },
  infoCardDescription: {
    fontSize: 16,
    color: "#555",
  },
});
